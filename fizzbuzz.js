use strict;

var fizzbuzz = function(num) {

  var outputText = "";

  if (num % 3 === 0) {
    outputText = "fizz";
  }
  
  if (num % 5 === 0) {
    outputText.concat("buzz");
  }
  
  if (output.length > 0) {
    return output;
  }
  
  return num;
}
